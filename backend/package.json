{
  "name": "pagoya-backend",
  "private": true,
  "description": "Reglas y pruebas de seguridad de Firestore para PagoYa",
  "scripts": {
    "test:reglas": "firebase emulators:exec --only firestore \"node test-reglas.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.20.0"
  }
}
